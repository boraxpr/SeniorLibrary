@page
@model SeniorLibrary.Pages.UserRoles.IndexModel
@{
    ViewData["Title"] = "UserRoles";
}
<h1>User Roles</h1>
<table class="table table-striped">
    <thead>
        <tr>
            <th>UserId</th>
            <th>Email</th>
            <th>Roles</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Model.userRolesViewModel)
        {
        <tr>
            <td>@user.UserId</td>
            <td>@user.Email</td>
            <td>@string.Join(" , ", user.Roles.ToList())</td>
        </tr>
        }
    </tbody>
</table>
